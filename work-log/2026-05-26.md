---
date: 2026-05-26
---

# Session Log — 2026-05-26

## Accomplished

### Dashboard: System Log fix
- Fixed the System Log tab — was calling `logread` (BusyBox syslogd tool), which doesn't exist on this router
- Changed to `cat /tmp/messages` — router uses syslog-ng, writes logs to `/tmp/messages`
- Deployed and verified working

### Xiaomi services: Discovery and shutdown
Identified and stopped 14 Xiaomi background services:

| Service | Function |
|---------|----------|
| `tbusd` | Xiaomi local communication bus — was spamming "Failed to connect to tbus[192.168.10.1]" every 5s |
| `trafficd` | Traffic accounting (no visible functionality in AP mode) |
| `miio_client` / `miio_proxy` | Xiaomi IoT cloud client for MiWiFi app |
| `miot` | IoT LED control |
| `xqbc` | QBC data collector — init script explicitly checks for AP mode before starting |
| `xq_info_sync_mqtt` | MQTT-based info sync (telemetry) |
| `messagingagent` | MQTT messaging agent |
| `mosquitto` | MQTT broker |
| `smartcontroller` | Smart controller |
| `cab_meshd` | CAP mesh daemon |
| `xiaoqiang_sync` | Xiaomi sync service |
| `milog` | Xiaomi logging |
| `miwifi-discovery` | MiWiFi discovery |
| `netapi` | Net API — **was already not running** (init script returns 0 in AP mode) |

### Dashboard: Xiaomi Services tab
New tab under **System > Xiaomi Services** with:
- List of all 14 known Xiaomi services
- Start/Stop buttons (live control)
- Enable Boot/Disable Boot buttons
- RSS memory column (reads from `/proc/PID/status` VmRSS)
- Persistent disable via `/data/dashboard/.disabled_services` file (on UBIFS, survives reboots)
- `disable_xiaomi.sh` boot script registered as firewall include

### Dashboard: Services tab (process monitor)
New tab under **System > Services** showing every running user-space process with:
- PID, Name, RSS memory, State, Command
- Sorted by RSS descending, total memory at top
- API endpoint: `action=ps_mem` reads directly from `/proc/*/status`

### Cleanup
- Removed **Tailscale** leftover nav entry and 5.8MB `tailscale.multicall` binary
- Removed **extra_wifi panel** files: `boot_setup.sh`, `deploy.sh`, `extra_wifi.cgi`, `vlan_wifi.sh`, `index.html`, `KNOWLEDGE.md`
- Kept `apply_vap.sh` (needed by dashboard Wireless tab for VAP create/delete)

## Technical Notes

### Router architecture
- ARMv7l, BusyBox v1.36.1, Linux 5.4.213, syslog-ng (not BusyBox syslogd)
- `/etc/` is **tmpfs** (not overlay) — regenerated fresh every boot
- This means rc.d symlink manipulation does NOT survive reboots
- `/data/` is UBIFS (persistent) — used for dashboard files and `.disabled_services`

### Service persistence
- `rm /etc/rc.d/S*svc* ` does NOT survive reboot (tmpfs)
- Firewall includes DO survive (UCI config stored in `/data/`)
- Solution: firewall include runs `/data/dashboard/disable_xiaomi.sh` at boot, which reads `/data/dashboard/.disabled_services` and stops listed services

### API design
- New `action_ps_mem` reads `/proc/*/status` for VmRSS — most reliable memory measurement
- Control characters in `/proc/PID/cmdline` break JSON — must sanitize with `tr` before `json_esc`
- All services use `data` wrapper for frontend consistency (`loadPage` passes `r.data`)

## What failed / Issues
- Initial "Disable Boot" implementation (rc.d symlink removal) doesn't survive reboots on this router — `/etc/` is tmpfs
- `json.DecodeError` on ps_mem endpoint due to control characters in `/proc/*/cmdline` — fixed with `tr` sanitization
- `renderServices` checked `d.code !== 0` but `d` is `r.data` (no `code` field) — fixed to check `d.services`
- `pidof miot` doesn't find the process because `miot` is an init script name, not a binary name

## Files changed
- `custom/dashboard/api.cgi` — added ps_mem, services, and Xiaomi service disable/enable
- `custom/dashboard/index.html` — added Services tab, Xiaomi Services tab, removed Tailscale
- `custom/dashboard/deploy.sh` — added disable_xiaomi.sh upload, re-added apply_vap.sh ref
- `custom/dashboard/disable_xiaomi.sh` — new file
- `custom/dashboard/tailscale.multicall` — deleted (5.8MB)
- `custom/extra_wifi/` — pruned to only `apply_vap.sh`
